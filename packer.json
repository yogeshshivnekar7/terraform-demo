{
  "variables": {
    "profile": "default",
    "region": "ap-south-1"
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "ap-south-1",
    "source_ami": "ami-0a4a70bd98c6d6441",
    "instance_type": "t2.micro",
    "ssh_username": "ubuntu",
    "ami_name": "packer-img {{timestamp}}"
    }],
  "provisioners": [{
    "type": "shell",
    "script": "./install.sh",
    "pause_before": "10s"
  }]
}
